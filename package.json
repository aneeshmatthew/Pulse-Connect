{
  "name": "pulseconnect-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["backend", "frontend", "shared"],
  "scripts": {
    "setup": "npm install && npm install --workspace=backend && npm install --workspace=frontend && npm install --workspace=shared",
    "seed": "npm run seed --workspace=backend",
    "start": "npm run start:all",
    "start:backend": "npm run build --workspace=backend && npm run start --workspace=backend",
    "start:frontend": "npx serve frontend/dist -l 5173",
    "start:all": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "build": "npm run build --workspace=shared && npm run build --workspace=backend && npm run build --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "serve": "^14.2.0"
  }
}
